//forEach => forEach is also a one of js function or mehtod it ittirates through each element of an array
//and it won't return new array insted it modify the existing array.

const data = [1,2,3,4,5];

data.forEach((dt) =>{
 let sm = dt * 2;
  console.log(sm);
})